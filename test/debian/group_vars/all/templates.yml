---
template1: my_string_1
template2: "{{ template1 }}"
template3: "{{ template2 }}"
template4: "inline+{{ template1 }}"
template5: "{{ template1 }}+inline"

special1: "äöü'!)§$;~é"
special2: 'ñô‰„}»"¯]¿¬'

list1:
  - first_list_item
  - second_list_item

dict1:
  first_key: first_value
  second_key: second_value

lookup_flattened: "{{ lookup('flattened', [1,2,3,[4,5,6]]) }}"

filter_zip: "{{ list1 | zip(anarchism_packages) | list }}"

filter_dict2items: "{{ dict1 | dict2items }}"
